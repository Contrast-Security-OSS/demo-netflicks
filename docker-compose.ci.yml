
services:

  # Development environment for Contrast Assess demos
  web-dev:
    image: "${STAGING_IMAGE_RUNTIME}"

  web-prod:
    image: "${STAGING_IMAGE_RUNTIME}"

  # Testing service
  tests:
      image: "${STAGING_IMAGE_TESTS}"
      volumes: 
        - /playwright-report:/tests/playwright-report


